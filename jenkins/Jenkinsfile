pipeline {
    agent {
        docker 
        {
            image 'python:3.9.1'
        }
    }
    stages {
        stage('Build') {
            
            steps {
                
                sh 'python -m pip install --no-cache-dir --user -r requirements.txt' 
            }
        }
        stage('Test') {
            
            steps {
                sh 'pytest'
            }
            post {
                always {
                    junit 'test-reports/results.xml'
                }
            }
        }
    }
}